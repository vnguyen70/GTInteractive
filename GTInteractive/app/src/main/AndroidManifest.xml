<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vi_tu.gtinteractive">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
<!--<<<<<<< HEAD-->
<!--=======-->
        <meta-data
            android:name="android.app.searchable"
            android:resource="@xml/searchable" />
<!--&gt;>>>>>> master-->

        <activity
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--<<<<<<< HEAD-->
        <activity android:name=".buildingDetail_info" />
        <activity android:name=".buildingDetail_events" />
        <activity android:name=".buildingDetail_dining"></activity>

        <activity android:name=".mapFilter_dining" />
        <activity android:name=".mapFilter_parking" />
<!--=======-->
        <activity
            android:name=".BuildingsTestActivity"
            android:label="@string/title_activity_buildings_test"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.vi_tu.gtinteractive.MainActivity" />
        </activity>
        <activity
            android:name=".DiningsTestActivity"
            android:label="@string/title_activity_dinings_test"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.vi_tu.gtinteractive.MainActivity" />
        </activity>
        <activity
            android:name=".EventsTestActivity"
            android:label="@string/title_activity_events_test"
            android:parentActivityName=".MainActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.vi_tu.gtinteractive.MainActivity" />
        </activity>
        <activity
            android:name=".MapActivity"
            android:label="@string/title_activity_map" />
        <activity android:name=".PrinterActivity" />
        <activity android:name=".BuildingDetailsActivity" />
        <activity android:name=".InternalBuildingActivity" />
        <activity android:name=".BuildingSearchActivity" />
        <activity android:name=".SearchableActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <provider
            android:name=".temp.SuggestionProvider"
            android:authorities="com.example.SuggestionProvider2" />

<!--&gt;>>>>>> master-->
    </application>

</manifest>